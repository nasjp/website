---
title: Minimum Prettier Config
datetime: "2024-08-27 02:25:00"
category: "memos"
status: "published"
---

Next.jsで必ず設定しているが、毎回調べるのが面倒なので、最低限の設定をまとめておく。

```bash
npm install --save-dev prettier-plugin-organize-imports
```

```bash
cat '{ "plugins": ["prettier-plugin-organize-imports"] }' > .prettierrc
```

```package.json
{
  "scripts": {
    "format": "prettier --write ."
  }
}
```